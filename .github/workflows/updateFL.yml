name: "Aktualizacja listy"

on: workflow_dispatch

jobs:
  Aktualizacja:
    runs-on: ubuntu-latest
    container: ghcr.io/filtersheroes/vichs_image:2.26.9-2
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - env:
          GIT_TOKEN: ${{ secrets.GIT_TOKEN }}
        run: |
          ./scripts/build.sh
